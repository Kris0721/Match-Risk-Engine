[package]
name = "ring-buffer"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbeam-utils = "0.8"
loom = { version = "0.7", optional = true }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "spsc_bench"
harness = false

[features]
loom = ["dep:loom"]
