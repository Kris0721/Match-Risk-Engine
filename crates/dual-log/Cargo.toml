[package]
name = "dual-log"
version = "0.1.0"
edition = "2021"

[dependencies]
core-types   = { path = "../core-types", features = ["serde"] }
wal          = { path = "../wal" }
crossbeam-channel = "0.5"
crossbeam-skiplist = "0.1"

[dev-dependencies]
tempfile = "3"
