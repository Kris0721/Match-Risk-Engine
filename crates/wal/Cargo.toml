[package]
name = "wal"
version = "0.1.0"
edition = "2021"

[dependencies]
core-types  = { path = "../core-types" }
rkyv        = { version = "0.7", features = ["validation"] }
memmap2     = "0.9"
crc32fast   = "1.4"
thiserror   = "1"

[dev-dependencies]
tempfile    = "3"