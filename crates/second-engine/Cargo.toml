[package]
name = "second-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
core-types   = { path = "../core-types" }
dual-log     = { path = "../dual-log" }
order-book   = { path = "../order-book" }
crossbeam-channel = "0.5"
